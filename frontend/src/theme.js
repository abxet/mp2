import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary: {
            main: "#1976d2", // Change this to your desired primary color
        },
        secondary: {
            main: "#dc004e", // Optional: Secondary color
        },
    },
});

export default theme;
